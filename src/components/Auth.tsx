import {useState} from 'react'
import {signUp,signIn} from '@/lib/auth'
export function Auth({onSuccess}:{onSuccess:()=>void}){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [isSignUp,setIsSignUp]=useState(false);const [loading,setLoading]=useState(false);const [error,setError]=useState('');const handle=async(e:React.FormEvent)=>{e.preventDefault();setLoading(true);setError('');try{const result=isSignUp?await signUp(email,password):await signIn(email,password);if(result.error){setError(result.error.message)}else{onSuccess()}}catch(err:any){setError(err.message)}finally{setLoading(false)}};return <div style={{display:'flex',alignItems:'center',justifyContent:'center',minHeight:'100vh'}}><div style={{width:'100%',maxWidth:'400px',padding:'2rem',border:'1px solid #ddd',borderRadius:'8px',background:'white'}}><h1 style={{fontSize:'2rem',marginBottom:'0.5rem'}}>Weekly</h1><p style={{fontSize:'0.875rem',color:'#666',marginBottom:'1.5rem'}}>a calm weekly planner</p><form onSubmit={handle} style={{display:'flex',flexDirection:'column',gap:'1rem'}}><div><label style={{display:'block',fontSize:'0.875rem',fontWeight:500,marginBottom:'0.5rem'}}>Email</label><input type="email" value={email} onChange={(e)=>setEmail(e.target.value)} placeholder="your@email.com" style={{width:'100%',padding:'0.5rem',border:'1px solid #ddd',borderRadius:'4px'}} required /></div><div><label style={{display:'block',fontSize:'0.875rem',fontWeight:500,marginBottom:'0.5rem'}}>Password</label><input type="password" value={password} onChange={(e)=>setPassword(e.target.value)} placeholder="••••••••" style={{width:'100%',padding:'0.5rem',border:'1px solid #ddd',borderRadius:'4px'}} required/></div>{error&&<div style={{fontSize:'0.875rem',color:'#dc2626',background:'#fee2e2',padding:'0.5rem',borderRadius:'4px'}}>{error}</div>}<button type="submit" disabled={loading} style={{padding:'0.75rem',background:'#d97706',color:'white',borderRadius:'4px',fontWeight:500,opacity:loading?0.5:1}}>{ loading?'Loading...':isSignUp?'Create Account':'Sign In'}</button></form><button onClick={()=>{setIsSignUp(!isSignUp);setError('')}} style={{width:'100%',marginTop:'1rem',fontSize:'0.875rem',color:'#666',cursor:'pointer',background:'none',border:'none'}}>{isSignUp?'Already have account? Sign in':'Need account? Sign up'}</button></div></div>}
